<!-- include header -->
<%- include('include/_header') %>
<!-- /include header -->

<!-- cart + summary -->
<section class="my-5">
    <div class="container">
        <div class="row justify-content-center"> <!-- Center the wishlist items -->
            <!-- cart -->
            <div class="col-lg-9">
                <div class="card border shadow-0">
                    <div class="m-4">
                        <h4 class="card-title mb-4">Your Wishlist</h4>
                        <span>wishlist count:</span><span style="color: red;"><%= cart.length %></span>
                        
                        <p></p>
                        <% if(cart.length > 0){ %>
                        <% for(var i = 0; i < cart.length; i++) { %>
                        <div class="row gy-3 mb-4" onclick="userdetails('<%=cart[i]._id%>')">
                            <div class="col-lg-5">
                                <div class="me-lg-5">
                                    <div class="d-flex">
                                        <img src="primg/<%= cart[i].image[0] %>" class="border rounded me-3"
                                            style="width: 96px; height: 96px;" />
                                        <div class="">
                                            <a href="#" class="nav-link">
                                                <%=cart[i].productName%>
                                            </a>
                                            <p class="text-muted">
                                                <%= cart[i].category %>
                                            </p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-lg-2 col-sm-6 col-6 d-flex flex-row flex-lg-column flex-xl-row text-nowrap">
                                <div class="">
                                    <small class="text-muted text-nowrap">₹ <%=cart[i].price%>/ per item </small>
                                </div>
                            </div>

                            <div
                                class="col-lg col-sm-6 d-flex justify-content-sm-center justify-content-md-start justify-content-lg-center justify-content-xl-end mb-2">
                                <div class="float-md-end">
                                    <a href="/api/addwtoc?productId=<%=cart[i]._id%>" class="btn btn-light border px-2 icon-hover-primary"><i
                                            class="fas fa-shopping-cart fa-lg px-1 text-secondary"></i></a>
                                    <a href="/api/removewish?id=<%=cart[i]._id%>"
                                        class="btn btn-light border text-danger icon-hover-danger"> Remove</a>
                                </div>
                            </div>
                            <% if(cart[i].catstatus==false||cart[i].deleted==true){ %>
                            <p style="color: red;">stock not available</p>
                            <% } %>
                        </div>
                        <% } %>
                        <% } else { %>
                        <p>No Items In Cart</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- cart + summary -->

<!-- include footer -->
<%- include('include/_footer') %>
<!-- /include footer -->

<!-- Bootstrap JS -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.7/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>

<script>
    function userdetails(id) {
        const productDetailURL = `/api/wishid?productId=${id}`;

        // Redirect to the user details page.
        window.location.href = productDetailURL;
    }
</script>
</body>

</html>
